# Used automatically by `vite build` (production mode). Relative paths —
# the built frontend and the API are served from the same origin by nginx
# (see nginx/dh.conf), so no absolute host/CORS round-trip is needed.
VITE_API_URL=/api/v1
VITE_MEDIA_URL=/media
